FROM node:20-alpine

WORKDIR /app

# Copiar package.json y package-lock.json
COPY package*.json ./

# Instalar dependencias
RUN npm ci

# Copiar todo el código fuente
COPY . .

EXPOSE 3001

# Para desarrollo: usar ts-node-dev para hot-reload
CMD ["npm", "run", "dev"]